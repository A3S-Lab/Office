Writer Phase 0 reviewable row grid-after property revisions.

### Writer
- Reviewable row-formatting for `gridAfter` with accept/reject, live track-changes, and native DOCX export (including current-row write)
- Opaque row fixtures moved to `wBefore`; broader `tcPr` / `trPr` shapes stay opaque or fail-closed

See CHANGELOG.md for the full engineering notes.
